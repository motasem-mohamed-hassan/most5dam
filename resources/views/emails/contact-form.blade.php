<!DOCTYPE html>
<html>
<head>
    <title>{{ $data['subject'] }}</title>
</head>
<body>
    <h1>{{ $data['name'] }}</h1>
    <p>{{ $data['email'] }}</p>
    <p>{{ $data['subject'] }}</p>

    <p>{{ $data['message'] }}</p>
</body>
</html>
